const chai = require("chai");
const { beforeEach } = require("mocha");
const expect = chai.expect;

// const { my-map } = require("../problems/my-map");

// describe("my-map", () => {
//     beforeEach(() => {
//         const arr = [1, 2, 3];

//         const callback = (el) => el * 2;
//     });
// })